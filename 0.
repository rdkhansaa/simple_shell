#include <stdio.h>
#include <ctype.h>

/**
 * main - function point
 *
 * Return 0 
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("this is a simple code by two software engineers,\n");
	}

	return (0);
}
